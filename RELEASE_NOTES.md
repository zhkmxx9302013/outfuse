# outfuse v0.3 — Release Notes

**版本**：0.3（versionCode 4） · **分支**：`V0.3`
**日期**：2026-08-18
**下载**：仓库根目录 `outfuse-0.3-release.apk` / `outfuse-0.3-debug.apk`

> outfuse 是一款个人媒体库播放器（Android / Jetpack Compose），支持本地、NAS（SMB）、WebDAV、网盘与 Jellyfin 来源，专注**个人正版媒体、无广告、隐私干净**的观影体验。

---

## ✨ 新功能

- **多窗口播放**：平板 / 横屏手机上可 2×2 同时播放最多 4 个视频，每个窗口独立播放/暂停/倍速/切换/关闭；播放结束自动播放下一个。
- **单窗口全屏**：多窗口中的任意视频一键进入全屏播放器，播放队列无缝衔接（无数量池限制），顶栏常驻「返回多窗口」按钮，切换带淡入/滑动动画。
- **继续观看弹窗**：打开视频时若上次进度超过 15 秒，询问「继续播放 / 从头播放」，三个内核（ExoPlayer / VLC / IJK）全部支持，按条目记忆选择。
- **缩略图磁盘缓存**：远程来源缩略图首次加载后落盘（160MB 上限、LRU 自动清理），大库滚动不再反复拉取网络，设置页可一键清除缓存。
- **图片查看器手势升级**：双指缩放/平移/旋转全部接入弹簧动画，旋转自动吸附 90°（0/90/180/270），操作丝滑。
- **视频旋转**：播放器设置支持 0°/90°/180°/270° 旋转，ExoPlayer / VLC / IJK 三内核统一生效。
- **NFO 刮削增强**：支持 `episode.nfo`、季/集信息、纯文本 NFO 兜底解析、Poster/Backdrop 侧车图大小写变体匹配。

## 🛠 稳定性与性能

- 海量文件（20 万+）导入不再闪退/丢失：分批扫描 + 大堆内存（largeHeap）+ 互斥锁与并发安全索引。
- 启动刷新不再卡顿；已删除文件快速同步（并发有界 1500 条）；播放/查看失败自动校验源文件，确认已删除才移除并 Toast 提示（网络抖动不误删）。
- 修复来源删除崩溃、全量扫描漏扫、重复条目、OOM、部分媒体"不支持播放重启后正常"等问题。
- 播放器内核错误链：ExoPlayer → VLC → IJK → Exo 回退，减少"不能播"的情况。

## 📦 安装

```bash
# Debug（可安装测试）
adb install outfuse-0.3-debug.apk
# Release（签名正式包）
adb install outfuse-0.3-release.apk
```

## 🧭 分支与历史

| 分支 | 说明 |
|---|---|
| `main` | 主线（最新稳定） |
| `V0.1` | 0.1 版本分支 |
| `V0.2` | 0.2 版本分支 |
| `V0.3` | **本版本**：多窗口播放 + 推荐优化（缩略图磁盘缓存、继续观看弹窗） |

## 📄 变更明细

完整中文/英文变更日志见 [`CHANGELOG.md`](CHANGELOG.md)。
