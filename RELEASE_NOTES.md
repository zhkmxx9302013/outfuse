# outfuse v0.3 — Release Notes

**版本**：0.3.7（versionCode 4） · **分支**：`V0.3`
**日期**：2026-08-19
**下载**：仓库根目录 `outfuse-0.3-release.apk` / `outfuse-0.3-debug.apk`

> outfuse 是一款个人媒体库播放器（Android / Jetpack Compose），支持本地、NAS（SMB）、WebDAV、网盘与 Jellyfin 来源，专注**个人正版媒体、无广告、隐私干净**的观影体验。

---

## ✨ 播放能力

- **三内核播放**：ExoPlayer（默认）/ VLC（网络 mp4/mkv/mov）/ IJK（flv/f4v 或回退），错误链自动回退减少"不能播"。
- **多窗口播放**：平板 / 横屏手机 2×2 同时播放最多 4 个视频，每个窗口独立播放/暂停/倍速/切换/关闭，结束自动播下一个。
- **单窗口全屏**：多窗口任意视频一键进入全屏，播放队列无缝衔接（无数量池限制），顶栏常驻「返回多窗口」按钮，切换带淡入/滑动动画。
- **继续观看弹窗**：上次进度超过 15 秒时询问「继续播放 / 从头播放」，三内核全部支持，按条目记忆。
- **视频旋转**：0°/90°/180°/270°，ExoPlayer / VLC / IJK 三内核统一生效。
- **图片查看器**：双指缩放/平移/旋转全部弹簧动画，旋转自动吸附 90°。

## 🎬 媒体库与发现

- **缩略图磁盘缓存**：远程来源缩略图首次加载后落盘（160MB 上限、LRU 自动清理），大库滚动不再反复拉取网络，设置页可一键清除。
- **NFO 刮削增强**：`episode.nfo`、季/集信息、纯文本 NFO 兜底解析、Poster/Backdrop 侧车图大小写变体匹配。
- **自建系列（收藏）**：详情页/图片查看器收藏图标弹出浮窗，支持多选系列、新建、重命名、取消收藏；首页按系列分组展示为 4 宫格缩略图卡片，点进单系列只看该系列内容。
- **媒体库筛选**：类型 / 来源 / 系列 / 排序多维筛选，顶部控件折叠收纳，默认只保留「筛选 / 排序 / 布局切换」，节省空间。
- **文件下载**：详情页下载按钮支持 SMB / WebDAV（网盘）/ 本地 / HTTP 下载到设置的下载目录，结果 Toast + 顶部提示。

## 🛠 稳定性与性能

- 海量文件（20 万+）导入不闪退/丢失：分批扫描 + 大堆内存（largeHeap）+ 互斥锁与并发安全索引。
- 启动刷新不卡顿；已删除文件快速同步（并发有界 1500 条）；播放/查看失败自动校验源文件，确认已删除才移除并 Toast（网络抖动不误删）。
- 修复来源删除崩溃、全量扫描漏扫、重复条目、OOM、部分媒体"不支持播放重启后正常"等问题。
- 读取媒体库时自动清洗历史版本写入的过时简介文案。

## 📦 安装

```bash
# Debug（可安装测试）
adb install outfuse-0.3-debug.apk
# Release（签名正式包）
adb install outfuse-0.3-release.apk
```

## 🧭 分支与历史

| 分支 | 说明 |
|---|---|
| `main` | 主线（最新稳定） |
| `V0.1` | 0.1 版本分支 |
| `V0.2` | 0.2 版本分支 |
| `V0.3` | **本版本**：多窗口播放 + 推荐优化 + 收藏/筛选体验完善 |

## 📄 变更明细

完整中文/英文变更日志见 [`CHANGELOG.md`](CHANGELOG.md)。
